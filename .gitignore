# OS
.DS_Store
Thumbs.db
desktop.ini

# Node (not used at runtime; one-off tooling)
node_modules/
*.log
package-lock.json

# Local dev servers
.serve/
.vscode/

# Downloads are not committed until the APK is real
# (the site treats a missing APK as "coming soon")
downloads/pausescroll.apk

# Editor / OS leftovers
*.swp
*~